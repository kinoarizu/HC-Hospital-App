part of 'shared.dart';

///* Material Theme Data
/// Berisi konfigurasi tema material design yang digunakan pada
/// aplikasi ini. Dan akan dipanggil pada root project `main.dart`.

ThemeData appTheme = ThemeData(
  primaryColor: accentColor,
  accentColor: baseColor,
);